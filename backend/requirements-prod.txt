# Web API
Flask==3.1.3
flask-cors==6.0.1
flask-talisman==1.1.0
python-dotenv==1.2.2

# Email parsing
python-oxmsg==0.0.2
olefile==0.47

# DNS, HTTPS and domain parsing
dnspython==2.7.0
requests==2.33.0
tldextract==5.3.1

# Detection engines
yara-python==4.5.4
dkimpy==1.1.8
# Advisory SPF re-evaluation (optional feature, off by default).
# pyspf prefers dnspython when importable and pulls in no DNS dependency
# of its own, but it calls the deprecated dns.resolver.query(), so the
# dnspython pin above is load-bearing for this package.
pyspf==2.0.14

# HTML parsing
beautifulsoup4==4.12.3
lxml==6.1.0

# Runtime
gunicorn==23.0.0
Flask-Limiter==3.8.0
redis==5.2.1
