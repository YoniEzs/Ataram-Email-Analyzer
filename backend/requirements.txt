# Flask and Extensions
Flask==3.1.3
flask-cors==6.0.0
flask-talisman==1.1.0
python-dotenv==1.2.2

# Email Parsing
extract-msg==0.48.0
olefile==0.47

# DNS and Network
dnspython==2.6.1
requests==2.33.0
tldextract==5.3.1

# WHOIS
python-whois==0.9.5

# Detection engines (all optional at runtime — analyzers degrade gracefully)
yara-python==4.5.4
pyspf==2.0.14
dkimpy==1.1.8

# HTML Parsing
beautifulsoup4==4.12.3
lxml==6.1.0

# Security
cryptography==48.0.1

# Production Server
gunicorn==23.0.0

# Rate Limiting
Flask-Limiter==3.8.0

# Shared cache / rate-limit storage (optional at runtime — used when
# REDIS_URL / RATELIMIT_STORAGE_URI points at a Redis instance)
redis==5.2.1

# Development Dependencies
pytest==9.0.3
pytest-cov==7.0.0
