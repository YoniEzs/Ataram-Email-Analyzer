// Optional deployment-specific settings. Keep empty for same-origin /api.
// Example for a separate HTTPS backend:
// window.ATARAM_CONFIG = { API_BASE_URL: 'https://api.example.com' };
window.ATARAM_CONFIG = window.ATARAM_CONFIG || {};
